services:
  rewardchart:
    build: .
    container_name: rewardchart
    restart: unless-stopped
    ports:
      - "3030:3000"
    volumes:
      - rewardchart-data:/app/data

volumes:
  rewardchart-data:
